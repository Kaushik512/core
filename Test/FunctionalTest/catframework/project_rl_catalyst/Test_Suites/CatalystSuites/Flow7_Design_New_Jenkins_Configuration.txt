*** Settings ***
Documentation     Organization_setup_Flow.txt

Resource           catalyst.txt
Resource           ${CATALYST TEST DATA}/${TESTDATA}
Resource           ${CATALYST ACTION}/Organization_Setup_Action_Steps.txt
Resource           ${CATALYST ACTION}/Design_New_Jenkins_Task.txt
Resource           ${COMMON RESOURCES}/catalyst_grid_node.txt


*** Test Cases ***
Catalyst new Jenkins Configuration setup
    [Tags]       Sanity               Smoke
#    Catalyst Login
    Catalyst User "${VALID USER}" logs in with password "${VALID PASSWORD}"
    Validate the Jenkins Configuration Setup
    Catalyst Create New Jenkins Configuration Setup          
    Catalyst Logout                      
    [Teardown]     Close Browser



Catalyst new Jenkins Executioon of Task Environment Setup
    [Tags]       Sanity               Smoke
#    Catalyst Login
    Catalyst User "${VALID USER}" logs in with password "${VALID PASSWORD}"
    Catalyst Creation of Jenkins Task in Environment
    Catlayst Creation of Chef Task in the Environment        
    Catalyst Logout                      
    [Teardown]     Close Browser



#-Catalyst Execution of Tasks by Creating the App
    #-[Tags]       Sanity               Smoke
#    Catalyst Login
    #-Catalyst User "${VALID USER}" logs in with password "${VALID PASSWORD}"
    #-Creation of New Application for your Project and Executing Tasks         
    #-Catalyst Logout                    
    #-[Teardown]     Close Browser

