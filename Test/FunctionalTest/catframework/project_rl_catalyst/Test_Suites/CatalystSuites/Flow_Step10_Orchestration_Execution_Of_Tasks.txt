*** Settings ***
Documentation     Orchestration_setup_Flow.txt

Resource           catalyst.txt
Resource           ${CATALYST TEST DATA}/${TESTDATA}
Resource           ${CATALYST ACTION}/Organization_Setup_Action_Steps.txt
Resource           ${CATALYST ACTION}/Orchestration_Action_Steps.txt
Resource           ${COMMON RESOURCES}/catalyst_grid_node.txt



*** Test Cases ***
Catalyst Creation of Chef Task in the Orchestration Tab on Imported Node
    [Tags]       Sanity       ps
#    Catalyst Login
    Catalyst User "${ORG ADMIN USER}" logs in with password "${ORG ADMIN PASSWORD}"
    Catalyst Create the ChefTask in the Environment and Execution
    Catalyst Logout
    [Teardown]     Close Browser  

Catalyst Execution of Chef Task from the Environment
    [Tags]       Sanity       ps
#    Catalyst Login
    Catalyst User "${ORG ADMIN USER}" logs in with password "${ORG ADMIN PASSWORD}"
    Catalyst Execution of the Chef Tasks Created
    Catalyst Logout
    [Teardown]     Close Browser  
